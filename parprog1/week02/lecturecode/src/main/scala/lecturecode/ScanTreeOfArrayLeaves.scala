package lecturecode

/**
  * scanLeft on trees is formulated to make the code as simple as possible. To
  * make the implementation more efficient, in practice we would not store
  * individual elements in the leaves of the tree, but use chunks of
  * elements represented using, for example, Arrays. Here's the implementation.
  */
object ScanTreeOfArrayLeaves {
  ???
}
